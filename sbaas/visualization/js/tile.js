tile = function () {
    // generic tile element
    this.id = '';
    this.posx = 0;
    this.posy = 0;
    this.width = 1;
    this.height = 1;
};
tile.prototype.set_id = function () {
    // set tile id
};
tile.prototype.set_pos = function () {
    // set x and y position
};
tile.prototype.set_width = function () {
    // set tile width
};
tile.prototype.set_height = function () {
    // set tile height
};
tile.prototype.add_datalist = function () {
    // add datalist (menu) for input
};
tile.prototype.add_draganddrop = function () {
    // add file drag and drop for input
};
tile.prototype.add_textarea = function () {
    // add text area for input
};
tile.prototype.add_checkbox = function () {
    // add checkbox for input
};
tile.prototype.add_color = function () {
    // add color pallet for input
};
tile.prototype.add_range = function () {
    // add range slider for input
};
tile.prototype.add_submitbutton = function () {
    // add submit button
};
tile.prototype.add_table = function () {
    // add button for output
};
tile.prototype.add_chart2d = function () {
    // add chart for interaction
};
tile.prototype.add_map2d = function () {
    // add chart for interaction
};